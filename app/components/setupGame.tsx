import React from "react";
import Button from "./button";

const SetupGame = () => {
    return (
        <div>
            <Button text="New Game" />
        </div>
    );
};

export default SetupGame;
